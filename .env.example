# Environment Configuration for MustardTree Partners Blog

# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# These variables MUST be set in production for secure authentication

# Admin User Configuration
ADMIN_USERNAME=webadmin
ADMIN_EMAIL=sam@mustardtreegroup.com
ADMIN_PASSWORD_HASH=your-bcrypt-hashed-password-here

# Application Environment
NODE_ENV=production

# =============================================================================
# HOW TO GENERATE SECURE ADMIN CREDENTIALS FOR PRODUCTION
# =============================================================================

# STEP 1: Generate a strong password (or use the provided script)
# Example strong password: MyS3cur3P@ssw0rd!2024#Blog
# Requirements:
# - Minimum 12 characters
# - At least 1 uppercase letter
# - At least 1 lowercase letter  
# - At least 1 number
# - At least 1 special character

# STEP 2: Hash the password using bcrypt
# You can use the following Node.js script to generate a hash:

# const bcrypt = require('bcryptjs');
# async function hashPassword(password) {
#   const hash = await bcrypt.hash(password, 12);
#   console.log('Password Hash:', hash);
# }
# hashPassword('your-chosen-password');

# STEP 3: Set the environment variables on your production server
# Example for Linux/Docker:
# export ADMIN_USERNAME="webadmin"
# export ADMIN_EMAIL="admin@mustardtreepartners.com"
# export ADMIN_PASSWORD_HASH="$2a$12$abcdefghijklmnopqrstuvwxyz123456789"
# export NODE_ENV="production"

# =============================================================================
# DEVELOPMENT ENVIRONMENT
# =============================================================================
# In development, credentials are auto-generated securely
# Check the console output when starting the application

# Development mode (default)
NODE_ENV=development

# Optional: Override default development email
ADMIN_EMAIL=dev-admin@mustardtreepartners.com

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. NEVER commit real passwords or hashes to version control
# 2. Use strong, unique passwords (minimum 12 characters)
# 3. Rotate credentials regularly
# 4. Use environment-specific configurations
# 5. Consider using a secrets management service for production
# 6. Always use HTTPS in production
# 7. Implement additional security measures like 2FA if needed

# =============================================================================
# DEPLOYMENT CHECKLIST
# =============================================================================
# Before deploying to production:
# ✅ Set ADMIN_USERNAME environment variable
# ✅ Set ADMIN_EMAIL environment variable  
# ✅ Set ADMIN_PASSWORD_HASH environment variable (bcrypt hashed)
# ✅ Set NODE_ENV=production
# ✅ Verify HTTPS is enabled
# ✅ Test login with production credentials
# ✅ Backup and secure credential storage